import React, { FC } from 'react';

interface NavUnlogggedWebProps {
  
}

const NavUnlogggedWeb: FC<NavUnlogggedWebProps> = ({ }) => {
  return (
    <div>NavUnlogggedWeb</div>
  );
};

export default NavUnlogggedWeb;