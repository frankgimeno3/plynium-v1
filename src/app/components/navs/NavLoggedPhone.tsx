import React, { FC } from 'react';

interface NavLoggedPhoneProps {
  
}

const NavLoggedPhone: FC<NavLoggedPhoneProps> = ({ }) => {
  return (
    <div>NavLoggedPhone</div>
  );
};

export default NavLoggedPhone;