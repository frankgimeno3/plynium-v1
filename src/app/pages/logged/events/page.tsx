import React, { FC } from 'react';

interface LoggedEventsProps {
  
}

const LoggedEvents: FC<LoggedEventsProps> = ({ }) => {
  return (
    <div>LoggedEvents</div>
  );
};

export default LoggedEvents;