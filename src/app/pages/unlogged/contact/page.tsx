import React, { FC } from 'react';

interface UnloggedContactProps {
  
}

const UnloggedContact: FC<UnloggedContactProps> = ({ }) => {
  return (
    <div>UnloggedContact</div>
  );
};

export default UnloggedContact;